#!/usr/bin/env bash

SCRIPTDIR=$(cd $(dirname "$0") && pwd)

if [ -f "$SCRIPTDIR"/madwizard ]; then
   export PATH="$SCRIPTDIR":$PATH
   "$SCRIPTDIR"/madwizard severalof $@
else
   madwizard severalof $@
fi
